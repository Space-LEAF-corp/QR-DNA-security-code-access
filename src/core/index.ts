/**
 * Core module exports
 */

export { ImmutableLedger } from './ledger.js';
export type { LedgerEntry, LedgerConfig, ValidationResult } from './types.js';
 * 
 * Provides the immutable ledger implementation with SHA-256 hashing
 */

export { ImmutableLedger, type LedgerEntry, type LedgerEntryOptions } from './ledger.js';
